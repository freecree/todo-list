import { FC } from 'react';
import './header.scss';

const Header: FC = () => {
  return (
    <header className='header'>
      <div className='wrapper'>Todo List</div>
    </header>
  );
};

export default Header;
